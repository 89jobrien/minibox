# rustqual config for minibox lib crate
# Test-heavy crate with cross-crate pub API consumers — tune accordingly.

ignore_functions = ["main", "test_*"]

# Bench, integration test, mock, and example files contain structural
# repetition and .unwrap()/.expect() by design.
exclude_files = [
    "tests/*",
    "benches/*",
    "src/adapters/mocks.rs",
    "src/testing/*",
    "examples/*",
]

# Cross-crate consumers (miniboxd, mbx, minibox-testsuite) use pub API;
# single-crate dead-code detection produces false positives.
[duplicates]
detect_dead_code = false

[complexity]
max_cognitive = 18
max_cyclomatic = 11
max_function_lines = 68

[srp]
# The only minibox functions above the workspace cap of 5 are daemon request
# boundary functions (handle_exec, handle_commit, and handle_build). Their
# dependency-injection/request signatures reach 9 parameters. Keep complexity
# analysis active with the strict thresholds above.
max_parameters = 9
file_length_baseline = 300
file_length_ceiling = 800
