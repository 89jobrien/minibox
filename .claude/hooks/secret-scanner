#!/usr/bin/env bash
# PostToolUse hook: delegates to secret-scanner binary.
set -euo pipefail

for dir in "$HOME/.local/bin" "$HOME/.cargo/bin" "/usr/local/bin"; do
  if [[ -x "$dir/secret-scanner" ]]; then
    exec "$dir/secret-scanner" "$@"
  fi
done

exit 0
