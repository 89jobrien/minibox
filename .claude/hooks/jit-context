#!/usr/bin/env bash
# UserPromptSubmit hook: delegates to jit-context binary.
set -euo pipefail

for dir in "$HOME/.local/bin" "$HOME/.cargo/bin" "/usr/local/bin"; do
  if [[ -x "$dir/jit-context" ]]; then
    exec "$dir/jit-context" "$@"
  fi
done

exit 0
