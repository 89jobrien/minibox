pipeline: minibox_cargo_fmt
# Auto-format Rust files after edit.
# Run: crux run .crux/cargo-fmt.crux
# With specific file: crux run .crux/cargo-fmt.crux --input '{"file":"src/main.rs"}'
budget: { calls: 4 }

steps:
  - step: fmt
    handler: shell::capture
    args:
      cmd: "cargo fmt --all"
