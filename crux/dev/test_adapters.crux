pipeline: test_adapters
steps:
  - step: run
    handler: shell::capture
    args:
      cmd: "cargo test -p minibox --test adapter_colima_tests && cargo test -p minibox --test daemon_handler_adapter_swap_tests"
