# Feature Matrix

The canonical, generated adapter capability matrix is
[`docs/core/FEATURE_MATRIX.mbx.md`](core/FEATURE_MATRIX.mbx.md).

Use that reference for current capability claims. In particular, Linux `native` bridge
networking supports both DNS configuration and port forwarding.
