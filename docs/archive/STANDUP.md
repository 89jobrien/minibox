> **ARCHIVED** — This document is not authoritative. See the current docs in the repo root.

# Standup Log

Append-only log of daily work sessions. Generated by `mise run all:standup`. Chronological order.

---

## 2026-03-22 10:30

**State at start:** Post-docs stable baseline (`8f6012f`), no LLM retry story, no agent tooling.

### Timeline

**2026-03-21**
```
12:00–13:00  Swept stale info across all user-facing docs  (files: README.md, USAGE.md, HANDOFF.md)
13:00–14:00  Baked CI env setup into composite action; drafted Firefox Profiler integration spec  (files: .github/actions/ci-setup/action.yml, .gitea/workflows/ci.yml)
17:00–18:00  Full minibox-llm timeout & retry arc — design spec → impl plan → HttpStatusError/is_transient → ProviderConfig fields → RetryingProvider w/ backoff → wired into FallbackChain → invoke!/ainvoke! ergonomic macros  (files: crates/minibox-llm/*, Cargo.lock; 8 commits 802ddf9..e7e70d2)
19:00–20:00  README touch-ups: cgroups description fix, platform adapter selection docs  (files: README.md)
```

**2026-03-22**
```
08:00–09:00  Added bench-diff, bench-report, profiling helpers, and error dedup to bench tooling  (files: bench/*)
09:00–10:00  Stood up Claude Agent SDK scripts (review, test-gen, diagnosis), agent run logging + rich terminal dashboard, council & standup agents, inline doc pass across all 9 crates, fixed standup file-count bug  (files: scripts/*, CLAUDE.md, crates/*/src/*.rs)
10:00–11:00  Added sync-check pre-push agent, commit-msg agent, restored git recipes in Justfile with sync-check gate, added mise interactive tasks, fixed standup timeline ordering  (files: Justfile, scripts/*, CLAUDE.md)
```

**Current state:** Full agent toolchain + LLM retry stack on main. Two open stashes (proxybox merge, observability WIP).

**Next:** Integration tests for `RetryingProvider`/`FallbackChain`, land the proxybox stash.

---

## 2026-04-12 11:07

## State at start
As of ~14:00 Apr 11, the **crux** project had just landed proptest suites and publish metadata — a freshly bootstrapped crate. **slash** was mid-parser development with urgency suffixes and builder args just landed. **minibox** and **devkit** were in maintenance/docs mode with no active feature work.

## Timeline

**2026-04-11**

```
01:00–02:00  devkit: handoff docs update (5a9472f)  (files: HANDOFF.devkit.workspace.yaml)
12:00–13:00  minibox: handoff docs update (1ab3c9a)  (files: HANDOFF.md, HANDOFF.minibox.workspace.yaml)
14:00–15:00  crux: proptest suites for ctx, delegation, task registry, replay, speculation (c5926b2)  (files: crates/crux-core/src/*)
15:00–16:00  slash: urgency as command-token suffix (dc6c742), optional/verbosity/builder args + Execute trait (4bf7b4e), handoff updates  (files: crates/lang/src/*)
16:00–17:00  slash: harden parser validation + proptest suite (f40c37a), restructure as Cargo workspace (aad1048)  (files: Cargo.toml, crates/cmd/, crates/lang/)
           crux: README, LICENSE, crates.io metadata (912a68c)  (files: README.md, LICENSE, Cargo.toml)
           minibox: pre-push commit range resolver hook (a48d8d9)  (files: .githooks/pre-push, docs/superpowers/specs/*)
17:00–18:00  slash: handoff updates (a44e057, de487e4)
19:00–20:00  slash: executor, Token enum, ParseError context, test quality (430d626)  (files: crates/lang/src/lib.rs)
           crux: fix markdown formatting in docs (426d6ba)
```

**2026-04-12**

```
05:00–06:00  slash: ProcessRunner implementation (c7e0848), README + LICENSE + crate metadata (0067906), CommandRegistry with builtin dispatch (8e15db4)  (files: crates/cmd/src/main.rs, README.md, LICENSE-*)
06:00–07:00  slash: executor redesign as AI agent tool orchestration (2487c33), lazy env/.slenv/dotenvx/secret detection (0c2da58), /obfsck builtin + --redact mode (dd8efc5), handoff update (d8a2227)  (files: crates/lang/src/*, crates/cmd/*)
07:00–08:00  crux: CLAUDE.md (07ada3f), rusqlite→redb migration for pure-Rust storage (4ce04d9), normalize join_all budget + failure hook semantics (23ae5cc), handoff update (73b2a1d)  (files: crates/crux-core/src/registry/redb.rs, Cargo.*)
09:00–10:00  crux: tighten lenient replay identity with content_hash filtering (23854a2)  (files: crates/crux-core/src/recorder.rs)
           slash: method validation on SlashCommand trait SCL-014 (6fdbce4)
10:00–11:00  crux: Phase 2 — macro wiring, tracing, streaming, checkpoint/resume (0bfadd0), handoff update (68e868e)  (files: crates/crux-core/src/*)
```

## Current state + next
**slash** has a working parser, executor, registry, and process runner — it's approaching a usable CLI tool with env/secret handling and builtins. **crux** completed Phase 2 (macro wiring, streaming, checkpoint/resume) and migrated to redb; Phase 2 items crux-13 through crux-17 are documented in HANDOFF. Uncommitted work exists in minibox (`Cargo.lock`, `standup.py` json fix), devkit (`meta.go`), and crux (`CHANGELOG.md`, `crux-script/`, `examples/`, `deny.toml`). Logical next: commit the crux uncommitted scaffolding, land the minibox `standup.py` bugfix, and continue slash toward an end-to-end invocation demo.

## Concerns
- **minibox `standup.py`** has a known missing `import json` bug (identified but uncommitted fix) — will break `mise run all:standup`.
- **crux** has significant uncommitted work (`crux-script/`, `examples/`) that should be committed before context is lost.
- **devkit** has an uncommitted `meta.go` change sitting for 24h+ — risk of drift.
- **slash** shipped 17 commits in ~20h — fast velocity but no CI workflow visible yet; regression risk without gates.

---
