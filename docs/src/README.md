# minibox

Minibox is a Rust container runtime with a daemon/CLI split, OCI image support,
Linux isolation, and swappable platform adapters. Start with the
[architecture overview](./architecture/overview.md), then use the crate pages
to find ownership boundaries.
