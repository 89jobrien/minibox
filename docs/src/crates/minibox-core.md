# minibox-core

Shared protocol, Unix-socket client, OCI image services, registry clients,
errors, events, preflight checks, and cross-platform adapters. Its `domain`
module re-exports the canonical types from `minibox-domain`.
