# minibox

Runtime application layer containing daemon handlers, server/state management,
Linux container primitives, and native/GKE/Colima adapters. It depends inward
on `minibox-core` and exposes compatibility re-exports for existing consumers.
